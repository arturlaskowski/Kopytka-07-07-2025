spring:
  application:
    name: customer-service
  jpa:
    hibernate:
      ddl-auto: update
  #  show-sql: true
    open-in-view: false
    properties:
      hibernate:
        "[format_sql]": true

server:
  port: 8581

services:
  payment:
    url: http://localhost:8582
