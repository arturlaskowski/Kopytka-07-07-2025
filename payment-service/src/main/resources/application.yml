spring:
  application:
    name: payment-service
  jpa:
    hibernate:
      ddl-auto: update
  #  show-sql: true
    open-in-view: false
    properties:
      hibernate:
        "[format_sql]": true

server:
  port: 8582