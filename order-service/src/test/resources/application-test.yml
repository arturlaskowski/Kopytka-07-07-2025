spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        "[format_sql]": true

services:
  customer:
    url: http://localhost:8581
  payment:
    url: http://localhost:8582